<!DOCTYPE html>
<html>
<head>
    <title>{% block title %}Bébou Crypto{% endblock %}</title>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}{% endblock %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>

    <style>
        .dark-mode {
            background-color: #12162b;
            color: white;

        }
        .dark-modebar {
            background-color:#0a0c17;
        }
        li {
            padding-left: 15px;
            padding-right: 15px;"
        }

    </style>
    <script>
        function myFunction() {
            var element = document.body;
            element.classList.toggle("dark-mode");
            var elements = document.getElementsByClassName('navbar navbar-default'); // get all elements.
            for(var i = 0; i < elements.length; i++){
                elements[i].classList.toggle("dark-modebar");
            }
        }
    </script>
    <script src="https://go-echarts.github.io/go-echarts-assets/assets/echarts.min.js"></script>
</head>
<body>
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="../../cryptos">Bébou Crypto</a>
        </div>

        <div class="collapse navbar-collapse">

            <ul class="nav navbar-nav navbar-left">




                <a class="navbar-brand" href="../../cryptos">{% trans %} Liste {% endtrans %}</a>
                <a class="navbar-brand" href="../../cryptos/categorie/Cryptocurrency">{% trans %} Categories {% endtrans %}</a>
                <a class="navbar-brand" href="../../cryptos/recherche/null">{% trans %} Recherche {% endtrans %}</a>
                <a class="navbar-brand" href="../../cryptos/recherche_avancer">{% trans %} Recherche Avancée {% endtrans %}</a>
                {%  if app.user %}

                    <a class="navbar-brand" href="/user/fav/{{ app.user.id }}">{% trans %} Favoris {% endtrans %}</a>

                {% endif %}


            </ul>

            <ul class="nav navbar-nav navbar-right">
                <li>
                    <div>
                {% for locale in locales %}
                    {%  if locale != app.request.locale %}
                        {% if locale == 'en' %}
                            <a class="btn btn-default navbar-btn"  href="{{ path('change_locale',{'locale':locale}) }}">English</a>
                        {% elseif locale == 'fr' %}
                            <a class="btn btn-default navbar-btn" href="{{ path('change_locale',{'locale':locale}) }}">Français</a>
                        {% endif %}

                    {% endif %}
                {% endfor %}
                    </div>
                </li>
                <li>
                    <div>
                        <a onclick="myFunction()" class="btn btn-default navbar-btn" >{% trans %} Mode Sombre {% endtrans %}</a>
                    </div>
                </li>
                <li>
                    {% if app.user %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <div>
                                <a href="/admin" class="btn btn-default navbar-btn">Admin</a>
                            </div>
                        {% endif %}
                    {% endif %}
                <li/>
                <li>

                    <div>
                        {%  if app.user %}
                            <a href="/logout" class="btn btn-default navbar-btn">{% trans %} Déconnexion {% endtrans %}</a>
                        {% else %}
                            <a href="/login" class="btn btn-default navbar-btn">{% trans %} Connexion {% endtrans %}</a>
                        {% endif %}
                    </div>
                </li>

            </ul>

        </div>


    </div>

</nav>

<div class="container">
    {% block body %}{% endblock %}
</div>
</body>
</html>
