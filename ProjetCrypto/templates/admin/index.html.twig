{%  extends 'base.html.twig' %}
{% block body %}
<div class="example-wrapper">
        <h1 style="padding-left:25px">Liste des utilisateurs</h1>
        <table class="table">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nom</th>
                <th scope="col">Roles</th>
                <th scope="col"></th>
            </tr>
            </thead>
            <tbody>
            {% set nb = 0 %}
            {% for user in users %}
                {% set nb = nb + 1 %}
                <tr>
                    <td>{{ nb }}</td>
                    <td>{{ user.username }}</td>
                    <td>
                        {% set nbR=0 %}
                    {% for role in user.roles %}
                        {% set nbR = nbR +1 %}

                            {{ role }}


                    {% endfor %}
                    </td>
                    {% if "ROLE_ADMIN" not in user.roles %}
                    <td>
                        <a href="{{ path('admin.ajouter', {id: user.id}) }}" class="btn btn-link">Promouvoir Admin</a>
                    </td>
                        {% else %}
                        <td>

                        </td>
                    {% endif %}
                </tr>
            {% endfor %}

            </tbody>
        </table>



    </div>
    &nbsp
    &nbsp
</div>
{% endblock %}
