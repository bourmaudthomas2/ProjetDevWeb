{% extends 'base.html.twig' %}

{% block body %}
    <h1>{% trans %}Ajouter un commentaire :{% endtrans %}</h1>

    {{ form_start(form) }}
    <br/>
    {{ form_widget(form) }}

    <div class="form-group">
            <button type="submit" class="btn btn-default">{% trans %}Enregistrer{% endtrans %}</button>
            <a class="btn btn-secondary" href="{{ path('crypto.index') }}">
                {% trans %}Liste des cryptomonnaies{% endtrans %}
            </a>
    </div>
    {{ form_end(form) }}
{% endblock %}